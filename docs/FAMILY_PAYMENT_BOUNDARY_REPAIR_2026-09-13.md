# Family-balance payment safety — September 13, 2026

## Current truth before edits

Rechecked the current family-payment route while parent-history PR #377 is under protected checks. The account is loaded with invoice/family detail before exact school/tenant proof, then cached role/center or guardian access is trusted. Unlike the invoice checkout service, both family-balance payment claims omit fresh actor authorization and expected topology. Before claiming, the route can cache school readiness, create/cache a Stripe Customer and save fee-consent fields. An existing Checkout resolver may expire a session before claim. Unknown legacy drafts are retried with missing request values reconstructed from current configuration; no immutable request digest or original retry deadline proves identical provider input. Late checkout responses use unconditional payment updates and can overwrite a newer outcome. These are current code findings, not claims that a real duplicate charge or breach occurred.

The independent read-only review reran 161 existing focused tests successfully. The actual family-route mock currently allows readiness writes and denies both claims, so it does not establish zero earlier side effects. No production financial action or data query was performed for these findings.

## Required safe completion

1. Resolve minimal exact account/family/school topology, reject mismatched supplied IDs, and prove current home identity separately from selected-school authority before reading financial detail or touching a provider. Reuse `authorizeBillingActorForTarget` for the initial snapshot and every claim; preserve legitimate payment-only parent access.
2. Inspect complete Stripe/Terminal draft conflicts before customer, consent or provider mutation. Ambiguous old attempts must remain held without resubmission, clearing history or reconstructing missing provenance. Known-session recovery requires exact provider/account/customer/payment identity validation before reuse or expiry.
3. Capture immutable family-payment preparation and exact provider-request digests, with the original bounded idempotency deadline. Claim before Customer creation; same-key retries must retain all actual provider parameters. Re-prove authority/topology immediately before provider submission. Do not renew a retry window or enable fallback modes with different keys.
4. Use locked fresh data for customer mapping and consent, remove incidental readiness-cache writes, and finalize monotonically with an atomic audit. Late failures/unknown responses must not regress a known Session, processing payment, webhook result or paid ledger. Reuse the existing atomic family-payment application for proven succeeded intents.
5. Extend real-route/service fake tests for stale users/devices/grants, moved/foreign topology, unknown legacy attempts, Terminal conflicts, multiple blockers, concurrent requests, lost responses, expired retry windows, audit rollback and webhook wins. Assert zero provider and local writes on denied or ambiguous preflight. Preserve partial-payment, agency-responsibility, historical-family and saved-method restrictions.

This is a separate code release after PR #377. No real charge/refund, consent repair, identity change, connected-account mutation, migration or provider publishing is authorized by this preparation. Production verification must be read-only or the existing exact reserved empty-denial probes.

## Implemented, pending protected release

The route now obtains a minimal authorized account/family/school target and current identity/device/grant proof before any financial or child detail, within RepeatableRead. All supplied IDs must agree. Malformed methods/amounts and cross-origin requests fail before details; parent saved-method charges are explicitly denied. Historical linked families retain one-time payment access. Requested amounts cannot silently change to a different payable amount. Family/agency responsibility and current collectable balance are rechecked inside every claim for both office and parent requests.

`family-payment-service.ts` owns complete Stripe/Terminal conflicts, account-serialized claims, exact topology and fresh-request authorization. A SHA-256 descriptor binds every provider parameter, customer preparation, API version, amount, scope and selected method to one Payment and its original 23-hour submission deadline. Customer creation uses a Payment-specific idempotency key after claim; checkout fallback modes are disabled. Changed or legacy unknown attempts remain held without reconstructed parameters or a replacement key. A known Session must pass raw customer/account/family/payment/amount validation before reuse or expiry; changed-principal replacement proves the original Session before creating the new claim.

All finalization takes Payment then BillingAccount locks, matching webhooks, rechecks exact current family/school/tenant ownership, and preserves freshly read fields. Provider I/O runs outside retried database transactions. Verified succeeded Intents use the existing atomic ledger/account/invoice application plus the same-transaction audit; PAID winners require the same provider receipt. Unknown/late failures cannot regress a Session, processing state or terminal result. A response arriving beyond the retry deadline can still settle without permitting another submission. Additional verification is described as such, not as a completed or ordinary processing payment. Readiness inspection no longer writes provider configuration; customer maps and disclosure acceptance merge only under the claim and locks. Request and response bodies are excluded from operational logging.

Focused validation: 163 tests passed in `output/wave25-focused-first.log`; the new service/API follow-up passed 36 top-level tests (35 service tests plus the 18-case actual-route harness). The initial service run caught three fixture mismatches: unsupported fake JSON-equality/numeric-range predicates and a customer-patch test that omitted its own unrelated field. Corrected the fake models to exercise actual predicates; no production guard was relaxed. An independent review found and verified corrections for late cached declines after unknown outcomes, stale processing, original-principal Session recovery and in-flight ownership changes. No source edits or provider calls were delegated.

The expanded follow-up passed 88 tests, including 40 service tests and the actual-route harness. The full gate passed 2,411 tests with Prisma generation, lint (one existing unrelated `_row` warning), typecheck and optimized Next.js 16.3.4 build in `output/wave25-vercel-build-final.log`. The first full gate's three stale source-shape assertions were updated to follow the new safety modules; its failure log is retained. Final integration review then restored instant-bank verification metadata used by existing webhook/settlement consumers and added an actual-route regression across card, instant-bank and ACH methods. The required release gate is repeated after that correction; record its final result below.

Fresh real-component Chromium and WebKit runs each passed 44 layouts at 320/390 pixels and 100/200% text plus eight recovery cases: 88 layouts and 16 recovery cases total. Evidence: `output/playwright/parent-payment-status-chromium-2026-09-14T01-10-27-418Z/results.json` and `output/playwright/parent-payment-status-webkit-2026-09-14T01-10-34-801Z/results.json`. Across both, 18 intercepted fake submissions and 96 fake read-only observations produced zero backend requests or browser errors. The primary integrator inspected current narrow/large-text screenshots. This is browser reflow, not native Dynamic Type, physical-device or live-payment evidence.

`npm run mobile:store:check` passed for both existing apps (`output/wave25-mobile-store-check.log`); no native source, dependencies or permissions changed. Protected production evidence is recorded only after deployment and guarded checks pass. No successful live payment execution is implied by the reserved fake-account denial checks.

Final release gate: `output/wave25-vercel-build-release.log` passed all 2,411 top-level tests, including the expanded 19-case actual-route harness, Prisma generation, lint, both TypeScript passes and optimized build. The process exited 0. This repeated gate includes the restored instant-bank metadata. The independent final integration review found no further concrete release blocker.

## Protected production release — verified

PR [#378](https://github.com/BrunerDigital/TheBEESuite/pull/378), candidate `5ad9a5d32c8b6e65e6453b78eacd28ca7445b5fb`, merged at September 14 01:27:21 UTC to `708f2541e634c109659af63a51550638ffbf89b1`. CI `34795712645` (clean install and production validation), CodeQL `34795711697`, all checks and review-thread inspection passed. Exact preview `dpl_HKYo1XjjhCG5EPbSvbBFbAfWyiyZ` was Ready before the protected merge; no bypass or force push was used.

Exact production `dpl_Hf55fHJF3CUzySCF4dV9zE5nzkPK` (`the-bee-suite-51y9yevsx-brunerdigital.vercel.app`) was Ready at 01:29:51.228 UTC on `thebeesuite.io`, `www.thebeesuite.io`, `the-bee-suite-beta.vercel.app`, `the-bee-suite-brunerdigital.vercel.app`, and `the-bee-suite-git-main-brunerdigital.vercel.app`.

Guarded reserved Parent/Teacher production checks passed at 01:36:52.557 UTC: 20 primary role cases plus mobile layout, history, date and privacy checks; two exact fake logins, 95 heartbeats, six reserved empty denials (including family payment), two status probes, three message-history, four update-history and four announcement probes. Zero blocked requests, HTTP errors or page errors. Six public invalid-payment-link cases also passed. Evidence: `output/playwright/app-review-production-after-parent-announcements-pr378/results.json`, `output/playwright/public-payment-invalid-pr378/results.json`, and `output/wave25-production-verification.log`. Both verification processes exited 0.

Health was `ok: true` / database-connected at 01:37:24.390 UTC. Since exact Ready, deployment-scoped error/fatal and 5xx groups were empty; the error-filtered build log showed only successful Build Completed. No successful live charge, refund, Customer/consent creation, management financial execution or real-data mutation was attempted. The financial success/recovery paths are supported by isolated actual-route/service tests, not claimed as live payment execution.
