# Parent message history — September 12, 2026

## Current truth before changes

Starts from PR #365 candidate `e4b389f1ee3ac08973429d02f387f23eaf1ad40c`; reconcile with protected main before release. Primary checkout, unrelated PR #311, identities, messages and production data remain untouched.

Source-confirmed: the parent server query takes only 20 newest messages without an ID tie-breaker, and the timeline slices the same 20 again. There is no history continuation. The timeline automatically scrolls to the bottom when message count changes. Reply links trust their URL subject and are not resolved independently of the latest page. The family-message POST checks the reply ID/family but retains the submitted subject; its parent family lookup lacks the shared tenant/current-child boundary. Signed attachment views and parent message props spread private storage/metadata fields that no display consumer needs.

This wave will add bounded, family-scoped earlier-message continuation, canonical linked replies, safe client display projections and read-only recovery that retains drafts and reading position. No real sends, storage uploads, identity changes or other product writes are authorized as tests. Daily report/photo history and service-day consistency remain a following wave, not silently completed by message pagination.

Verification must cover equal-timestamp keyset paging, foreign/removed/inactive scope, malformed cursors/receipts, canonical deep links, private no-store responses, retry/stale requests, and actual Chromium/WebKit mobile layouts. The release requires full gates, protected exact-head merge, Ready production, aliases, health/logs and guarded reserved-demo verification.

## Implemented and locally verified

Reconciled cleanly with protected PR #365 main `3f396ffcd28059bb099d6ca96163995e43f0b840` before validation. The primary checkout and unrelated work remain preserved.

- Added private/no-store, Parent-only GET history in 20-message keyset pages, ordered by timestamp and ID. The cursor is resolved inside the same current guardian/family/tenant/child predicate as its content; missing or foreign cursors fail without restarting. A RepeatableRead snapshot rechecks current family access. Authorized pickup and staff cannot use this Parent endpoint.
- Earlier messages retain the reading anchor, unsent body and selected file. Duplicate requests are blocked; expired, malformed, wrong-family and failed responses preserve the current conversation with retry guidance. Family changes and server refreshes abort/discard stale responses. Message timestamps include the day. Initial loading still presents the newest messages first.
- Deep replies resolve their actual scoped database record independently of the newest page. URL subjects are ignored. Unavailable targets explain how to recover without silently substituting another message. Parent POST derives the canonical reply subject and rechecks it at commit.
- The Parent display DTO explicitly contains only public message/attachment fields. Raw metadata, sender identities/roles, storage keys/buckets and internal Supabase URIs are not sent to the client. Failed attachment signing exposes no private storage location. The shared attachment projection preserves all actual UI consumers.
- Parent sends repeat current guardian, tenant, enrollment, role, reply and teacher assignment inside a Serializable commit after upload. A school change during preparation rejects the send. Proven rollback removes only newly uploaded objects from the message bucket; unknown commit outcomes retain recoverable objects. This is not a new outbound-message idempotency guarantee.
- Current teacher delivery requires the same tenant, school, classroom and currently enrolled family child. Historic sender IDs do not authorize moved/inactive staff. Leadership recipients require a current operations role plus a matching tenant/CENTER grant with valid dates or the existing current staff-school leadership assignment. A reply's existing operations sender/assignee remains eligible only with current school leadership scope, current same-tenant brand/regional authority, or current platform-owner authority, matching `auth.ts`; this is not a broadcast to all operations users. Canonical school scope drives upload, templates, delivery and audit. Centerless families resolve only one unambiguous current school; ambiguity blocks sending with a clear explanation while retaining read access.
- Internal replies are limited to their exact internal thread; ordinary authorized staff subjects remain editable. App Review delivery remains portal-only. No production message, upload, notification, role or identity mutation was exercised.

Focused regressions pass 27/27, including actual GET/POST handlers with isolated mocked database/provider boundaries. Cases include 41 equal-timestamp messages, a newer insertion between pages, cross-family/tenant/removed/inactive denial, current teacher/leadership delivery, forged replies, centerless-school ambiguity, concurrent school/access/subject changes, known rollback cleanup and unknown commit recovery. Two old source assertions were updated to follow the shared predicate rather than incorrectly require its former inline location; authorization expectations remain enforced.

Chromium and WebKit each pass 12 actual-component cases at 320/390 px and 100/200% text: all 41 messages, 44px load control, unchanged reading anchor within 2px, retained draft/file/reply, four retry cases, stale-family and stale-server responses, disabled duplicate control, ambiguous-school read-only recovery and long attachment names. Each engine made 19 fake GETs, zero writes/off-origin requests and no client exceptions. Evidence: `output/playwright/parent-message-history-{chromium,webkit}/results.json` and fake screenshots. These are browser fixtures, not authenticated native/device tests.

Visual review caught issues a zero-overflow assertion missed: at doubled text the school header collapsed into a narrow column, the reply subject clipped, the composer pushed Send outside its visible area, and a second attachment's remove control required hidden horizontal scrolling. The scoped message layout now uses fixed decorative spacing and 44px action columns, wrapping school/reply/attachment text and wrapping attachment chips. Text remains enlarged. The privacy indicator is a shield rather than an online-looking dot. Stronger browser assertions check actual text rectangles, a usable composer width and every action's full bounds, not only page overflow. The standalone harness now loads the application's Geist font and verifies it, eliminating its initial accidental browser font fallback. Singular history status text is also corrected.

The independently reviewed guarded production checker passes 32 offline safety tests. Exact named reads and same-origin heartbeats require verified fake authentication; named reads also require a one-shot allowance. After this wave's exact Ready deployment it must verify the real Parent read endpoint, Teacher denial, missing-cursor rejection, exact public/error envelopes and the unavailable deep-link's actual hidden submission subject. It conditionally verifies a real continuation if a scoped cursor exists. Existing demo conversations are expected to be empty; populated history/pagination is proven by local fixtures, not invented production evidence. Full production gate and protected release are pending.

Final local production gate passed: `npm run vercel-build` completed Prisma generation, lint (zero errors; one pre-existing unrelated warning), TypeScript, all 2,155 tests and the optimized production build. Evidence: `output/wave13-vercel-build-release.log`. The first gate caught stale source assertions; a subsequent gate caught a TypeScript-only deferred-response helper issue in the browser harness. Both were corrected and the complete gate rerun. Native store-readiness also passes; no native source or capabilities changed. Protected PR, exact-commit production and guarded live checks remain release gates, not inferred from this local result.

Protected PR #366 review identified two real compatibility cases before merge. Staff-family queries now use the actor's exact authorized message-school IDs; only platform owners may cross the identity tenant, preserving their existing authorized all-school workflow without widening ordinary staff access. Parent replies retain historical operations recipients only when their current authority still permits that school; moved/inactive/downgraded or foreign-tenant non-platform recipients remain excluded. Leadership grants cannot preserve delivery after downgrade to Parent, Teacher, billing or auditor roles. Actual POST regressions cover these paths. Independent read-only authorization review found no blocker. The corrected candidate passed the full production gate again, including all 2,155 tests, lint, types and optimized build (`output/wave13-review-vercel-build.log`); focused regressions also pass 27/27. The earlier passing candidate is not being merged unchanged. Protected CI and live verification remain pending.

## Protected release and live verification — September 12, 20:00 UTC

PR #366 merged at 19:53:19 UTC after corrected head `9566e76f2a33cb6d113a796069c1a30e66cf0743` passed required validation (`34715207006`), all CodeQL checks (`34715205894`), exact-head Vercel preview and both resolved review conversations. Merge commit: `b6237035ffe74b3d2489684f037697fb8f706f44`.

Production `dpl_2bCYX8asgdrsS2Ft1XR3CnLXXpn5` is Ready on that exact merge commit. All five canonical aliases match: `thebeesuite.io`, `www.thebeesuite.io`, `the-bee-suite-beta.vercel.app`, `the-bee-suite-brunerdigital.vercel.app` and `the-bee-suite-git-main-brunerdigital.vercel.app`. `/api/health` returned healthy/database connected at 20:00:22.613 UTC. Build errors and scoped error/fatal/5xx logs since 19:56:11 UTC were empty after changed-flow verification; this is an observation window, not a claim about future errors.

Fresh strict reserved fake graph proof preceded two successful App Review logins. Guarded live checks completed at 20:00:04.033 UTC: Parent history 200 with exact public envelope and private/no-store caching, missing cursor 400 without restart, unavailable linked reply without forged subject/alternate target, and Teacher history 403. Four live message-layout checks passed at 320/390px and 100/200% text. The fake conversation contains zero messages, so populated live continuation was explicitly NOT performed; both-engine local 41-message fixtures provide that separate evidence.

All 20 Parent/Teacher phone/desktop routes, 52 Parent fit checks, eight shared-shell cases, four Teacher task-navigation cases and earlier profile/document/report/home/history/shortcut checks also passed. Allowed requests were two logins, 57 same-origin session heartbeats, six empty-body billing denial probes, two read-only payment-status GETs and three read-only message-history GETs. No messages, uploads, financial records, identities or other product data were mutated; zero blocked requests, HTTP errors or client exceptions. Evidence: `output/playwright/app-review-production-after-parent-message-history/results.json` and `output/wave13-production-verification.log`.

This web release is verified. Native store-readiness remains static only for this wave; no new signing, authenticated simulator/device, archive, TestFlight or Store submission is claimed. Broader school activation, general management-role authentication and the remaining product backlog are separate, still-open mission work.
