# Production Readiness Continuation Prompts

Copy the matching prompt into each completed readiness task. These prompts continue from the task's existing audit and evidence. They do not authorize deployment, production mutation, billing activation, live communications, credential changes, or invented signoffs.

## 1. User and role permissions

Continue the User and role permissions workstream from your completed audit. Re-read the current shared worktree and `docs/BRENDENS_TASKS.md` first because other readiness tasks have changed overlapping files. Complete every remaining repo-safe next step you can without waiting for Brenden: add the two-school credentialed role-matrix plan and reusable evidence template; strengthen automated isolation coverage for executive, director/billing, teacher, parent, kiosk, public, family, and classroom boundaries; investigate and propose the safest code-backed resolution for implicit executive tenant-wide access, persistent kiosk PIN rate limiting, trial-tenant creation, and authorized-pickup routing. Implement only changes that are clearly safe and consistent with current product policy; otherwise document the decision with a recommended default. Run focused tests, typecheck, and diff checks. Add only true Brenden-only approvals, accounts, or policy decisions to `docs/BRENDENS_TASKS.md`, without duplicating existing entries. End with what you completed, remaining gates, and the single next executable action.

## 2. User experience and flows

Continue the User experience and flows workstream from the existing audit. Reconcile the current shared worktree first. Complete all repo-safe work possible before asking for input: expand automated coverage for login, reset, parent setup, payment-link expiry, cancellation, failure, retry, receipt, and preserved-input recovery states; audit keyboard focus, accessible names, alert announcements, touch targets, zoom, and responsive layouts on the critical public flows; fix clear defects and add regression tests. Create a selected-school credentialed UX/device evidence checklist that can be executed once accounts are approved. Do not use live family data, mutate billing, or deploy. Update `docs/BRENDENS_TASKS.md` only for decisions or real-device/account actions that genuinely require Brenden. Run focused tests, typecheck/lint where contention allows, and diff checks, then report the next executable action.

## 3. School setup and onboarding

Continue the School setup and onboarding workstream. Start by re-running the read-only readiness audit and reviewing changes made by the other workstreams. Improve `pilot:check` so it supports selected-school filtering and explicit module gates while keeping setup, invitations, kiosk activation, and billing separately controlled. Add or improve a reusable per-school reconciliation/evidence packet for identity, classrooms, staff, families, children, assignments, guardians, access, counts, exceptions, and approvals. Complete any safe automated checks or tests that do not create accounts, send invitations, change production data, or enable billing. Preserve ProCare as source of truth until written cutover approval. Put only actual wave choices, accepted owners, and signoffs in `docs/BRENDENS_TASKS.md`. End with current readiness evidence and the exact next action after Brenden selects the first school.

## 4. ProCare migration

Continue the ProCare migration workstream from the completed audit. Re-check the current importer and overlapping changes, then complete the next repo-safe safeguards: design and implement an automated source-versus-target reconciliation report where possible; improve relationship-safe batching and duplicate review for exports over 500 rows without weakening fail-closed behavior; define and test reversal/rollback evidence requirements; and add retention/deletion controls or documented enforcement points for raw import rows and backups. Use synthetic fixtures only. Do not import production data or change the source of truth. Keep the exact-export HMAC/hash protections intact. Update `docs/BRENDENS_TASKS.md` only with secure export, mapping, owner, and approval actions that require Brenden. Run focused tests, typecheck, lint if available, and diff checks, then give the exact preview-only pilot action.

## 5. Payments and Stripe Connect

Continue the Payments and Stripe Connect workstream. Re-read the current shared worktree and existing audit. Complete every repo-safe next step: implement an explicit per-school business approval gate in addition to technical Stripe readiness; design and implement a read-only payout/balance-transaction reconciliation workflow with tests and clear mismatch states; add a reusable per-school Stripe evidence packet; and strengthen automated coverage for requirements, capability regressions, payment setup, failures, refunds, ledger application, payout reconciliation, and billing remaining off until separately approved. Follow current Stripe guidance and do not onboard accounts, enable billing, charge/refund money, contact users, or deploy. Add only school selection, human owner, external Stripe onboarding, and signoff items to `docs/BRENDENS_TASKS.md`. Finish with tests and the next safe action for the first school after Kokomo.

## 6. Enrollment CRM

Continue the Enrollment CRM workstream. Reconcile current overlapping registration and onboarding changes first. Complete repo-safe work for the inquiry-to-enrollment handoff: add automated isolation tests across two schools; make the distinction between CRM stage labels and approved enrollment records unmistakable in UI, API, and documentation; add delivery/backup/retry evidence templates for school notifications and Google Sheets; and test school-prefilled application handoff, invalid schools, missing access, duplicates, and approval boundaries. Do not send live notifications, create enrollments, or deploy. Add only approved accounts, real mailbox evidence, school selection, and human signoff to `docs/BRENDENS_TASKS.md`. Run focused and repository gates where possible and end with the exact credentialed smoke sequence.

## 7. Parent experience

Continue the Parent experience workstream with highest priority. Re-read the current parent onboarding, invitation, auth, and communications changes. Implement the safest repo-scoped one-time setup-link flow and existing-account credential transition that removes shared-password parent provisioning, including expiry, single use, replay denial, reset/recovery, audit state, and tests. Do not invalidate real credentials, send invitations, or deploy. Also investigate multi-family guardian behavior, parent-document gating, receipt standards, and three-identity linked/unlinked/cross-school isolation; fix clear code defects and document policy decisions with recommended defaults when human approval is required. Update `docs/BRENDENS_TASKS.md` only for approval, live account, test-family, and signoff actions. Run focused tests, typecheck, lint if possible, and diff checks, then state the safe release and credentialed-smoke sequence.

## 8. Teacher experience

Continue the Teacher experience workstream. Start from the current classroom-boundary fixes and reconcile overlapping daily-report changes. Design and implement a user-scoped, idempotent offline queue with safe account switching, tenant/classroom scoping, encrypted/minimal local payloads, exactly-once or idempotent replay, logout cleanup, conflict handling, and tests. Then complete the health-check workflow and Teacher location UI if their requirements can be established from existing product behavior and docs; otherwise build the reusable evidence/test scaffolding and record the smallest policy decision needed. Restore typecheck/lint, preserve cross-classroom fail-closed behavior, and add tablet/connectivity regression coverage. Do not use production child data or deploy. Put only custody policy, device, account, and human acceptance decisions in `docs/BRENDENS_TASKS.md`.

## 9. Director experience

Continue the Director experience workstream. Reconcile the overlapping teacher daily-report change and restore a clean typecheck first. Then complete repo-safe Director coverage for school setup, family/staff operations, CRM approval, attendance, incidents, documents, FTE, reports, alerts, billing, refunds, and reconciliation. Add a reusable selected-school Director smoke and signoff packet, strengthen automated authorization and error-recovery tests, and fix clear defects without touching production data or billing. Coordinate assumptions with the permissions, payments, reporting, and onboarding artifacts already in the worktree. Add only school selection, real test accounts, financial evidence, and named signoff ownership to `docs/BRENDENS_TASKS.md`. End with passed gates and the exact credentialed Director smoke sequence.

## 10. Corporate dashboards

Continue the Corporate dashboards workstream immediately from the identified isolation risk. Replace unsafe tenant-wide empty filters with visible-center constraints across child, enrollment, messaging, attendance, and forms views. Add two-tenant and limited-grant isolation tests, including empty-grant and stale/invalid filter cases, while preserving intentionally authorized corporate visibility. Review all related dashboard links, KPIs, CRM, FTE, billing, imports, users/grants, reports, and audit views for the same pattern and fix confirmed instances. Read the current Next.js guide before code changes. Run focused tests, typecheck, lint where possible, and diff checks. Do not mutate production or deploy. Add only credentialed executive account and human signoff actions to `docs/BRENDENS_TASKS.md`, then report the exact corporate smoke sequence.

## 11. Communications

Continue the Communications workstream from the signed SendGrid Event Webhook implementation. Complete repo-safe work without sending mail or changing provider settings: add durable `sg_event_id` deduplication before any event-driven side effects; add accepted-stale, deferred, suppressed, bounced, and needs-follow-up reporting; verify redaction and signature-failure behavior; strengthen webhook replay, batching, malformed-event, and state-transition tests; and document the safest default for tenant credential fallback. Create a precise provider-configuration and evidence checklist for SPF, DKIM, DMARC, reply inboxes, suppressions, webhook signing, and accepted-to-delivered/failed proof. Do not configure SendGrid or send live messages. Add those external actions and legal classifications to `docs/BRENDENS_TASKS.md` without duplication. Run focused tests and repository gates, then state the exact authorized test sequence.

## 12. Reporting and analytics

Continue the Reporting and analytics workstream. Re-read the current report, FTE deep-link, and dashboard changes. Build a reusable per-school reconciliation packet and automated test harness for approved source totals, cutoff dates, definitions, filters, tenant/school scope, exports, FTE deep links, and freshness timestamps. Add two-school isolation and export-equivalence tests using synthetic fixtures; fix any confirmed scope, filter, traceability, or stale-data defects. Make report definitions and data-as-of times visible where currently ambiguous. Do not use real unapproved data or deploy. Add only source-total approval, cutoff-date selection, credentialed accounts, and human acceptance to `docs/BRENDENS_TASKS.md`. Run focused tests, typecheck/lint, and diff checks, then give the exact reconciliation procedure for the selected first-wave school.

## 13. Apple and Google app readiness

Continue the Apple and Google app readiness workstream. Complete everything repo-safe and platform-independent while Brenden decides Apple-first versus Android: validate Capacitor configuration, app IDs, deep links/universal links, icons/splash assets, permission strings, privacy manifests/disclosures, push registration/error handling, release versioning, and native workflow test plans; add automated configuration checks and a physical-device evidence packet. Fix clear repository defects without signing, uploading, submitting, or changing store accounts. For Apple, prepare exact Mac archive/TestFlight/privacy-report steps; for Android, prepare the corresponding signed-build/internal-test steps but do not initiate them. Add only store-owner, signing credential, account, platform-choice, and device signoff actions to `docs/BRENDENS_TASKS.md`. Finish with the exact first physical-device smoke sequence.

## 14. Security and compliance

Continue the Security and compliance workstream from the pending RLS migration. Reconcile current migrations and tests, then complete repo-safe protections: expand automated RLS/grant drift checks; add credentialed isolation test scaffolding; define database-plus-Storage restore evidence and deletion/retention drill packets; audit secret handling, log redaction, payment boundaries, medical/custody data, audit events, and CSP readiness; and implement clearly safe controls or regression tests. Do not apply migrations, rotate credentials, change Supabase settings, or touch production data. Use current Supabase/Postgres guidance. Add only migration authorization, leaked-password/MFA settings, restore drills, legal policy approvals, and external evidence to `docs/BRENDENS_TASKS.md`. Run focused security tests, audit, typecheck, and diff checks, then state the separately authorized release verification sequence for 86/86 RLS coverage.

## 15. Performance and QA

Continue the Performance and QA workstream. Wait for or safely identify completion of competing workspace processes, then rerun `npm run vercel-build` on the unchanged candidate without stopping unrelated work. Build the repo-safe QA package that is still missing: measurable performance thresholds with recommended defaults, synthetic load scenarios, role/device regression matrix, recovery/offline cases, responsive target viewports, and repeatable evidence capture. Execute non-production tests that do not require approved credentials; classify failures by owning workstream and avoid claiming those domains ready. Add only owner assignments, selected-school devices/accounts, and threshold approvals to `docs/BRENDENS_TASKS.md`. End with build results, tests, unresolved defects, and the exact credentialed/device/load sequence.

## 16. Deployment and ops

Continue the Deployment and ops workstream after confirming concurrent workspace jobs have finished. Stop only verified stale processes from this workspace if needed, then rerun `npm run ops:check` and `npm run vercel-build` on the unchanged candidate. Complete repo-safe operational artifacts: monitoring/alert test plan, database migration backup/restore/forward-recovery drill packet, cron execution evidence template, incident/escalation matrix, release stop conditions, rollback decision record, and post-release smoke checklist. Do not deploy, connect to production for mutation, apply migrations, rotate secrets, or change Vercel settings. Add only named owner acceptance and external drill/configuration actions to `docs/BRENDENS_TASKS.md`. End with whether the candidate build is clean and the exact separately authorized release sequence.

## 17. Rollout and training

Continue the Rollout and training workstream using the completed per-school control record and the current `docs/BRENDENS_TASKS.md`. Review every other readiness audit now present in the shared worktree and consolidate their current BLOCKER, REQUIRED BEFORE WAVE, and FOLLOW-UP items into a single per-school launch-control view without erasing domain ownership or marking unevidenced work complete. Standardize every safe artifact you can: owner acceptance form, training attendance and competency evidence, support schedule, severity/response targets, stop/rollback/re-entry rules, launch-week review, and signoff packet. Keep setup, parent invitations, kiosk, billing, payments, and ProCare cutover independently gated. Do not schedule people, contact schools, send invitations, enable billing, or invent signatures. Deduplicate `docs/BRENDENS_TASKS.md` so it contains only real Brenden decisions/actions. End with the smallest ordered Brenden task sequence that unlocks the most workstreams and the exact first-school launch-control action.
