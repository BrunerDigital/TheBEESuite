# Office billing mobile flow — September 13, 2026

## Current state before changes

- Isolated branch `work/office-billing-mobile-flow-20260912` began clean at protected PR #370 main `f1f1ca21f8345cc73b430e3d0913a65435a37213`. PR #369 was live and guarded-account verified; #370 was deploying at the baseline and has since passed its independent release checks. Existing dirty primary checkout and unrelated tasks remain untouched.
- Independent read-only Chromium fixtures reproduced five overlapping primary Billing task labels at both 320/390px and 100/200% text. Expanded More tasks reproduces this across eleven actions. The shared tab default `flex-1` and nowrap labels squeeze the list into equal tiny cells instead of wrapping each task.
- Terminal's implicit mobile grid column expands to a 541px minimum content track, making the page 557px wide on 320/390 phones at 200% text. Explicit zero-minimum sizing fixes page overflow but would still conceal the full selected family. Selected values, action labels and the truncated Payment Review context require wrapping too.
- Baseline screenshots/metrics: `output/playwright/office-flow-read-only-audit-tabs` and `output/playwright/office-flow-read-only-audit-terminal`. Root visually checked both reproduced failures. Eighteen fake-component Chromium cases used production CSS without AppShell; no backend/API/non-GET/off-origin requests or exceptions occurred. Test-only CSS experiments are diagnostic, not implemented-product evidence.
- General management-role QA credentials remain invalid; do not retry/reset accounts or imply production office verification from public health. Existing reserved Parent/Teacher accounts support shared regression checks only.

## Implementation and evidence contract

Scope tab sizing to the Billing task list; preserve every primary/More action, selected panel, keyboard operation, disabled state and existing draft guards. Keep at least 44px targets and complete labels rather than hiding/truncating text. Scope Terminal reflow to its workspace: zero-minimum tracks/children, full family/invoice/reader values and full protected Payment Review context. Let columns respond to the available width and enlarged text. Preserve exact family/invoice/amount locks, provider behavior, payment confirmation and all money/access approval gates.

Validate actual components with real CSS at 320/390/768/1280/1440 and 100/200% text, long names, all eleven Billing tabs, More collapse/active selection, keyboard selectors and full review context. Reuse the existing fake recovery fixtures and cleanup helper. Add focused regression tests, both browser engines, relevant existing draft/payment-lock checks, full production gate and protected release. Actual-shell and production verification are distinct; record unavailable management authentication honestly. No payment, provider, identity or school activation changes are needed.

## Implemented reflow and recovery

Billing task labels wrap at their natural width while every More task and selected panel remains available. Terminal tracks collapse with enlarged text; selected family/invoice/reader values and Payment Review context are no longer truncated. Shared task-directory columns adapt to available width without changing destinations. Office summary metrics, Ledger family/date controls and additional tuition charge rows use zero-minimum adaptive grids. Large decorative mobile gutters/icons shrink without reducing user text or touch targets. Inputs, actions and checkbox labels keep at least 44px targets.

Expanded testing reproduced and corrected Ledger's clipped desktop family selector at 1280px/200%, 40px tablet date targets, clipped print actions, oversized summary icons/metrics and internally cropped native dates at 320px/200%. Filled Ledger dates retain enlarged text with fixed 10px inline padding. Family badges have auto height and bounded rounded-rectangle corners: ordinary box measurements alone missed text masked by oversized pill corners. Root and independent review visually checked these fixes.

Steady keyboard testing reproduced header/bottom-navigation overlap in both engines, including a fully obscured invoice description. Office roots now schedule the existing active-only visibility helper after two animation frames. It rechecks that the initiating control is connected, still focused and not an open popup; it never calls focus. Scoped scroll margins and the short-phone 12px top margin match the shell's actual header behavior. Independent reruns passed 16 cases / 136 focused input, button and textarea observations with no shell occlusion. Evidence: `output/playwright/wave18-office-focus-corrected-{chromium,webkit}`.

The committed fixture uses real components/AppShell, production CSS order, Geist and exact local branding assets. It rejects API, non-GET and off-origin requests. All eleven task panels, empty/populated fake refund states, selectors, full labels, dates, More selection and shell focus are exercised. No Save, Charge, Refund, email or provider action is invoked. Existing broader recovery QA now loads both real CSS layers and preserves each run in a timestamped directory; both engines passed all existing checks with 30 locally intercepted fake attempts each and no real writes.

Four new regression tests cover adaptive layouts, hidden-tab preservation, readable selectors/dates/badges and deferred focus ownership. The final focused suite passes 58 tests; independent review passed another 22-test subset. The first full gate passed all 2,186 tests and compilation but caught a TypeScript key-inference issue in the QA-only asset map; its explicit string-key type is corrected and the entire production gate is being rerun. Failed/intermediate evidence is preserved, not labeled a final pass. Static Parent/Teacher store readiness passes. Final browser/build/protected-release results follow when complete.

## Final candidate gates

The full rerun `npm run vercel-build` passed Prisma generation, lint (zero errors, one existing unrelated warning), TypeScript, all 2,186 tests and the optimized Next.js build. No tests, type checks, lint or security controls were disabled. Evidence: `output/wave18-vercel-build-final.log`; focused 58-test log `output/wave18-focused-final.log`; static native/store log `output/wave18-mobile-store-check.log`.

Final Chromium and WebKit each passed 42 actual-shell scenarios, including all 11 panels in each of 14 Billing variants (154 panel checks per engine). The final in-repository keyboard loop includes Terminal, Billing and Ledger: 126 Chromium and 90 WebKit focused-control observations, all clear of shell overlays. Every run verifies stable SHA-256 hashes for the nine relevant source files, local branding loaded, filled date values, zero API/non-GET/off-origin requests and zero browser exceptions. Root visually checked corrected phone dates, tabs, enlarged labels and Terminal views. Evidence: `output/playwright/office-billing-mobile-chromium-2026-09-13T22-09-06-297Z` (completed 22:11:13 UTC) and `output/playwright/office-billing-mobile-webkit-2026-09-13T22-09-19-905Z` (completed 22:12:32 UTC). These supersede intermediate runs with an incomplete fake asset server, script assertions, or source changes during execution. Protected release remains next; authenticated management production verification is still unavailable.

## Protected deployment and guarded production regression

Protected PR [#371](https://github.com/BrunerDigital/TheBEESuite/pull/371) merged September 13 at 22:19:16 UTC. Candidate `1b07c980da22358306c493b35b42508192e7854a` passed CI `34786163110`, all CodeQL analyses and its exact Ready preview `dpl_83HABV8qi1sU4CS9RcnPc8TeRmUT`. Main merge `a942b997c4dd6b32b05c42f6064e2b28a7d322a2` reached production `dpl_2ssNpkh3HTr5zTWA2xt6wbiEqyzi`, Ready at 22:22:18.886 UTC. All five canonical aliases were confirmed: `thebeesuite.io`, `www.thebeesuite.io`, `the-bee-suite-beta.vercel.app`, `the-bee-suite-brunerdigital.vercel.app`, `the-bee-suite-git-main-brunerdigital.vercel.app`. No bypass or force push.

Fresh reserved fake graph and matched Parent/Teacher sign-ins passed guarded production regression at 22:28:57.275 UTC: 20 routes, 52 Parent fit checks, eight shared-shell checks, four Teacher navigation checks, four message-history and four message-layout checks, 12 report-target checks, 16 child-picker checks, four Updates GET outcomes and four date/layout sequences. Two logins, 91 verified heartbeats, six exact empty-body denial probes, two status reads, three message-history reads and four Updates reads were admitted. No blocked requests, HTTP errors, client exceptions or product/financial/message writes. Evidence: `output/playwright/app-review-production-after-parent-updates-pr371/results.json` and `output/wave18-production-verification.log`.

Health reported database connected at 22:29:50.355 UTC. Post-verification deployment-scoped error/fatal and 5xx queries returned no entries; error-filtered build logs contained only build completion. The office UI is deployed, but its authenticated management-role production flow is **not verified**: existing general QA credentials remain unavailable and were not retried/reset. Local actual-component office evidence and shared Parent/Teacher production regression are distinct proofs.

## Separate next technical work

The pre-existing invoice due-date label formatter uses browser timezone while the date input uses UTC calendar-date semantics; this can display September 13 beside a September 14 input. Reproduce and correct the shared due-date display/print path in the next bounded wave without changing actual payment/ledger timestamps or stored dates. Legacy family-balance provider-attempt safety also remains separate technical work; no money or authorization behavior was changed by this UI release.
