# Teacher individual-task child context — September 12, 2026

## Current truth before edits

Branch `work/teacher-individual-targets-20260912` begins from reviewed PR #367 candidate `b454a271bae8ad62b31f8cacecaaa9dac1cb3e8e`. Reconcile its protected main merge before this wave's final gates. The primary checkout, unrelated PR #311, running server, earlier stashes and production data remain untouched.

Independent actual-Next fake baseline at 20:01:18 UTC contains 12 phone cases and 24 screenshots at 320/390px and 100/200% text: `output/playwright/teacher-individual-targets-baseline/results.json`. Every expanded Incident state has no selected-child name and no selector: its name is only in a collapsed summary that disappears on expansion. Location shows the selected name/current location but has no nearby child selector. Photo has a picker, but selected/option names use the old truncating styles. No API/off-origin/write attempts or client errors occurred. Original phone captures and explicitly taller clear crops are recorded separately.

All three tasks share `selectedChildId`. Existing `chooseChild` validates current roster membership, blocks while pending, confirms affected unsaved classroom drafts, preserves multi-recipient report batches, and changes exact IDs only after confirmation. The roster remains an individual-child selector; report-only selection stays separate. The API still independently authorizes current school/classroom access. No new backend, permission, roster search or data query is needed.

Implement one reusable controlled full-name picker in Photo, Incident and Location, with unique labels/IDs, brief shared-context guidance, current-roster options only, empty/pending disabling and the existing `chooseChild` callback. Preserve `photo-child` and add `incident-child` / `location-child`. Do not add independent state, fallback children, direct setters or synchronization effects. Confirm/cancel, same-child no-op, exact intercepted payloads, pending response safety, long names, empty/removed rosters and both-engine enlarged-text/keyboard flows must pass before release. No real incidents, moves, photos, notifications or identity changes are permitted as tests.

## Implementation and verification in progress

Reconciled with protected PR #367 main `8687feaf040718af614496e1dbc7aa460901a31c`. Three instances of `TeacherChildPicker` now expose the existing shared exact child, using only current `byClassroom` options and `chooseChild`. There is no new state, first-child fallback, effect, API, roster expansion or permission change. A removed child becomes explicitly unselected, preserving the existing draft and requiring a guarded choice; an empty roster explains the school-office next step. Pending operations disable all three pickers.

Incident's general review guidance now follows its child picker, making the target visible before explanatory copy. Independent actual-Next review then reproduced an arrival overlap at 320×568 and 200% text: the selected name extended about 6px under bottom navigation, and the picker about 17px. Short screens already place the header in normal document flow but retained its old sticky scroll clearance. A teacher-only rule, under the exact existing short-screen/header condition, uses 12px block-start clearance for task anchors and keyboard controls. It preserves bottom clearance, taller/sticky layouts, parent layouts and enlarged text; no font is shrunk.

Focused tests pass 29/29, including a regression for the conditional scroll offset. The component fixture checks full names/options, 44px targets, accessible labels/descriptions, keyboard close/focus return, no-op/cancel/confirm transitions, single-versus-multiple report recipients, staff-only/backdated settings, independent profile drafts, exact intercepted child IDs, rejection retention, pending locks, removed children and empty rosters. Both-engine final runs and actual-Next offset verification are being completed; earlier passing evidence is preserved separately.

The broad recovery fixture passes in Chromium and WebKit with 30 locally intercepted synthetic attempts per engine and no backend writes. The harness now waits for the previous report option popup to finish closing before opening Photo, avoiding overlapping option locators without weakening the exact-name assertions. The new child-picker harness uses PR #367's shared browser/server cleanup boundary. Static `mobile:store:check` passes for both native apps; no native source, entitlements or assets changed. Full production gate, protected release and guarded live read-only picker checks remain pending.

## Keyboard layout-shift correction

A final original-viewport WebKit case exposed a second issue at 390×844 / 100%: confirming a child change correctly cleared affected drafts, but removing the 118px unsaved-draft banner moved the focused Location picker to 18–62px, underneath the sticky header ending at 132px. Earlier tall-crop resize artifacts were separated from this reproducible product defect; failed evidence is retained.

The initiating picker now waits for Base UI's documented installed `onOpenChangeComplete(false)` callback and two animation frames before checking its own trigger. The narrow helper scrolls only when that trigger is still connected, still owns focus, its popup is closed and it is covered by the sticky header or bottom navigation. It never calls `focus()`, changes a target, observes another picker or overrides a subsequent interaction. Existing CSS scroll margins and instant scrolling preserve the native reduced-motion behavior. Relative short-screen headers are not treated as sticky.

The exact original-viewport WebKit reproduction now keeps Location at 148–192px below the header, adjusting scroll position by the removed banner/gap. Four focused regressions cover top/bottom occlusion, visible controls, relative headers, moved focus, open popups, disconnected controls and the closing instance's callback. Focused correction checks pass 11/11; Chromium and WebKit each rerun all 34 child-picker cases successfully with four locally intercepted fake Incident/Location requests, zero product writes, blocked requests or client errors. Photo's exact multipart target remains source-proven and its draft selection/retention is browser-tested; no production upload is used as evidence.

The optional `--teacher-individual-targets` production checker adds 16 read-only geometry, full-name, option, focus-return and shared-local-selection checks, retaining all report-target checks. It does not change `allowRequest`, strict fake graph preflight, matched login or allowed request counts. Its 32 offline safety tests pass. Live execution still requires the exact Ready release and independent review.

## Final release candidate — September 12, 20:33 UTC

Final consolidation removes the repeated expanded child-name descriptions from Photo and Location. All three forms show the exact name once in their own picker; Location preserves its current-location explanation immediately afterward. Collapsed summaries still identify the child and relevant sharing/location status. No actions, settings or content are removed.

The focus helper ignores a hidden desktop bottom-navigation rectangle; a fifth helper regression covers that case. Normal teacher buttons/comboboxes already have header-aware CSS scroll margins, so nearest scrolling is proven by actual rendered geometry, not merely a unit assertion that a method was invoked. The live checker now measures against the rendered sticky/fixed header plus inset and visible navigation minus inset, not the raw viewport edges.

Final focused suite: 34/34. Final actual-component harness: 34/34 each Chromium and WebKit (`output/wave15-release-browser-*.log`), four locally intercepted fake Incident/Location writes each, zero product writes, blocked requests or client exceptions. Independent actual-Next final-release evidence passes 37 visual/keyboard cases (19 Chromium including desktop hidden navigation; 18 WebKit), eight navigation/history cases, 43 visible-focus assertions and all six cancel/confirm paths. Every affected source hash remained stable. Evidence: `teacher-individual-targets-final-release-*` and `teacher-task-navigation-wave15-final-release-*` under `output/playwright/`; original failures and intermediate passes remain separately labeled.

The final production gate passes Prisma generation, lint (zero errors; one pre-existing unrelated warning), TypeScript, all 2,169 tests and optimized Next.js build: `output/wave15-release-vercel-build.log`. The 12-page PR #367 print packet was rendered and all pages visually reviewed. Protected PR/CI and exact-commit production verification for this new child-picker release are the remaining technical release steps; no signing/device/store evidence is implied.
