# Directory and draft-safety continuation — September 12, 2026

## Baseline before changes

- Fresh isolated branch `work/school-directory-draft-safety-20260912` started at `origin/main` commit `2cadf48c47da929bdbf596b4a9580caed5803a6c`. Prior wave PR #352 was subsequently merged and production-verified at `7e36706c`, incorporated normally without force-push.
- Team Permissions silently loads at most 250 users and 300 sessions. Sessions depend on the truncated user IDs. The user count is therefore not the complete authorized count.
- A visible user can carry active grants and a staff profile outside the actor's selected workspace. The directory's nested queries do not filter those details. The session-revoke endpoint has a separate, less precise scope predicate.
- Automation New workflow retains the old values, workflow switching overwrites drafts, and a transport failure has no handled recovery. No provider activation, real message, identity change, or session revocation is authorized as part of this code work.
- Billing draft/context and date-only defaults, older parent records, and other role navigation remain separate actionable follow-up work.
- Concurrent school-setup PR #353 (`09f2cc34`) was incorporated normally. The validated PR #352 branch was also incorporated for shared recovery/guard code, followed by its protected main merge. Unrelated work remains preserved.
- A read-only fake-data teacher test then reproduced silent draft reassignment: selecting child B after preparing a photo, incident, and report for child A retained all content but changed recipients without confirmation. A 42-child selection silently discarded two children. These became priority corrections in this wave before automation work.

## Released and production-verified

Protected PR #355 merged as `40c48a9aaa9d2f06d211862bb7e90093612698f3` on September 12 at 04:19 UTC. CI and CodeQL passed; Vercel deployment `dpl_73M8wfzhCChdcktnrJF4RxHGJC3Z` is Ready with `thebeesuite.io`, `www.thebeesuite.io`, and the existing project aliases. `/api/health` reported `ok: true`, database connected at 04:23 UTC. Scoped runtime error/fatal counts were empty after deployment; filtered build logs reported completion.

Post-release reserved fake-account checks passed 20/20 Parent/Teacher navigation cases and 2/2 Teacher report-control cases at phone/desktop sizes. Fresh fake-scope proof passed both accounts; zero HTTP errors, client exceptions, overflow, blocked requests, or product writes. Only two logins and 12 known session heartbeats were performed. Evidence: `output/playwright/app-review-production-after-pr355/results.json` and 22 fake-only screenshots. General directory authenticated UI remains unverified because its separate QA credentials are unavailable; actual scoped read-only Prisma queries and local real-component tests cover that path. No physical-device or signed-native claim is made.

- Team directory: server search, independent 50-row directory/session pages, complete scoped counts, stable ordering, no false-empty database fallback, and nested school-level privacy filtering. Unused IP/browser/revoker data is no longer sent to the client.
- Teacher: explicit discard confirmation for affected drafts before changing child/report recipients; attendance no longer changes drafting selection; missing roster recipients fail closed; oversized report batches show an error without changing the prior selection; controls lock during pending requests; file input clears with a successful or explicitly discarded photo draft. Enlarged summary text can reflow into readable columns.
- Backdated/staff-only report settings survive successful or queued saves and ordinary recipient changes. Destination-only location drafts are guarded and clear after explicit discard or confirmed/queued save. No content is silently sent when choosing targets.
- Session actions use the directory's authorization predicate, fail closed on concurrent access changes, and explain pending/success/uncertain outcomes. Read-only users and the current device receive explanations instead of unexplained disabled controls. Tenant-wide nested profile/grant projections validate the referenced tenant as well as the root record.
- The directory component and existing record-label/stat-card presentation were extracted into narrow reusable modules without changing default shared presentation. Compact directory metrics and enlarged text reflow reduce unnecessary vertical space.
- No real sessions revoked, reports/photos/incidents sent, identities changed, messages delivered, or provider/financial actions taken.

## Validation history (earlier pending statements below are historical)

- Focused scope, pagination, and teacher draft tests pass; read-only exact Prisma queries against the verified isolated demo scope passed for center-scoped and tenant-wide users/grants/staff profiles and paginated sessions. No live rows or credentials are included in evidence.
- Real-component local browser harness passes 23 behavior groups, including staff-only backdates, recipient cancellation/discard, pending controls, canonical directory search/paging, read-only/current-device explanations, and session recovery/success announcements. Ten write attempts were intercepted entirely in the localhost fixture; none reached an API/provider/database. A malformed HTTP 200 cannot announce that a session ended.
- Directory screenshots/checks cover 320/390/768/1440px at 100%/200% text. Parent/Teacher home density passes **96/96** across Chromium/WebKit; all six teacher actions fit above the navigation at 320px default text, and enlarged KPI labels reflow cleanly. No API/off-origin/write attempts, uncaught client errors, or document overflow. Evidence is browser-only, not physical-iOS evidence.
- Static Parent and Teacher `mobile:store:check` passes. Full production gate and protected release are still pending for this wave; authenticated general-role QA remains unavailable because its securely stored credentials failed, and no identity reset is inferred.
- Both Windows-safe `ios:parent:sync` and `ios:teacher:sync` completed successfully, refreshing bundled launch/offline assets and SPM plugin references. Installed core/CLI/iOS packages match at 8.4.1, with App plugin 8.1.0. Native doctor reaches the expected environment boundary: Xcode is not installed on this Windows host. Unsigned macOS CI, signed archives, and physical-device checks are separate evidence.
- Initial full gate correctly failed when an exact-source assertion still expected the now-shared label formatter inside the former page module. The test now verifies both consumers import the shared formatter and directly tests every preserved acronym/default behavior. Focused 16/16 pass; the full gate is rerunning without disabling any check.
- The corrected full gate passed **1,985 tests**, lint/types, and the optimized build (166 static pages). Final review then added atomic device revocation/audit and strict success receipts; the final gate must run again on that exact code. Mocked real-route tests prove access denials, exact scoped mutation rechecks, concurrent-change conflict, rollback on audit failure, and one audit on confirmed retry. No production session was revoked.
- Final code gate passed after those safeguards: Prisma generation, ESLint, TypeScript, **1,986/1,986 tests**, and optimized production build (166 static pages). The local browser harness also passed again with ten intercepted requests, including malformed-success handling. Protected PR/deployment verification remains the next release step.

Use focused scope/pagination/draft tests and fake browser fixtures, followed by the full production gate. Release only through protected merge, exact-commit Vercel Ready, aliases, health/log checks, and available authenticated changed-flow verification. Do not confuse mocks, a public health response, or unsigned simulator launch with school activation or signed-device acceptance.
