# Parent Updates history — September 12, 2026

## Current truth before changes

Isolated branch `work/parent-updates-history-20260912` begins at reviewed PR #368 candidate `ac2560378ab6a60904729f45f846c15b1488d7d6`; reconcile its protected main merge before final release gates. Current verified production is PR #367 main `8687feaf040718af614496e1dbc7aa460901a31c`. The freshly rendered/visually reviewed 12-page fake-data browser snapshot is `output/pdf/BEE_Suite_Current_Mobile_UI_2026-09-12_PR367.pdf`, captured 16:22–16:25 America/New_York. It precedes this work. Primary checkout, other PRs/worktrees/stashes and production records remain untouched.

Independent source review reconfirmed that the parent page loads only 20 sent reports and 20 shared media rows, and the date selector is built solely from those rows. Older days are unreachable and a busy day can be incomplete. Media groups/displays upload `createdAt`, not capture `takenAt`. Signing currently spreads private storage fields into the serialized media props. Report attendance enrichment can fetch an unbounded range spanning the oldest/newest loaded reports. Server service-day and client display-timezone selection differ. Home's latest-report value is taken from the newest mixed report/photo day, so a newer photo-only day hides the latest report; today's report status also comes from the capped list.

## Implementation contract

- One selected school day; separate bounded report/photo streams (50 rows plus one continuation probe), stable timestamp/ID ordering and scoped cursor anchors. No hidden global row cap, no unbounded whole-day fetch and no combined cross-table cursor.
- Date input and nearest earlier/later nonempty-day navigation. Ordinary document links preserve the existing parent navigation/unsaved-change contract and allow exact-day Back/reload. Explicit empty days remain empty. Separate load-more controls retain content and allow safe retry.
- Require the current guardian/family relation, current children, one consistent school and tenant. Repeat current-family/child/school predicates on every record/cursor/day probe and attendance aggregate. Ambiguous, removed, payment-only or contradictory ownership fails closed.
- Resolve the school location timezone once. Validate real calendar dates before existing timezone helpers; test 23/25-hour DST days. Use photo `takenAt` everywhere in history. Keep report `sentAt` for the existing chronology helper.
- Return explicit display DTOs only: no storage keys, internal URI, uploader, raw metadata or record spreads. A failed signed photo URL becomes null with retained caption/date and visible recovery text.
- Keep Home's latest sent report and current-day reported-child status independent of historical selection. Use bounded attendance aggregation instead of loading raw multiweek logs.
- No schema migration, real fixture creation, identity changes, messages, uploads, incident acknowledgments, payments or provider changes. Fake local route/browser fixtures only; live verification requires fresh reserved fake graph proof and read-only requests.

## Required evidence

Actual handler tests with mocked read-only boundaries: more than 21 days; 101 reports/photos in a day (50/50/1 without gaps); equal timestamps; invalid/foreign/withdrawn cursors; calendar/DST/capture-vs-upload dates; current-family/school/tenant denials before content or signing; exact public fields and signing failure; bounded first/last attendance; independent Home results. Browser evidence in Chromium/WebKit at 320/390 and 100/200% text, full names, dates, keyboard, retries, stale family/snapshot responses and preview no-request boundaries. Preserve timezone assertions when replacing the old date combobox. Run focused and full production gates, protected release, exact Ready aliases/health/logs and guarded live changed-flow checks. Until then this wave is not released or complete.

## Implemented candidate — September 12

Reconciled with protected PR #368 main `8ee717d5df8b66a0d0dfec7c3925d57717b44fc3`, now live and verified. Parent Updates uses the same scoped server reader for initial rendering and GET continuation. Reports and photos each use 50+1 stable descending rows, exact day/cursor predicates and complete guardian/family/current-child/school/tenant filters. The selected day has explicit nearest nonempty Earlier/Later links and a Latest recovery. An explicit empty date stays empty. Capture `takenAt`, not upload time, controls photo placement and display. First/last attendance comes from a same-day scoped aggregate, not raw multiweek logs.

Home reads its latest sent report and today's reported-child aggregate independently. Its report shortcut opens that exact school day; today's ready link uses the server-observed school date. Unavailable reads show an explicit unknown state and retry rather than falsely claiming no report. A sanitized operational event records snapshot failure without family/user/child identifiers or error messages. Home does not fetch/sign history photos; Messages, Payments and Family do not execute the new history/Home reader. Existing authorized non-guardian portal preview remains on its previous capped path and cannot use the guardian-only API. The two disabled legacy parent streams use truly empty IN predicates, not magic IDs. No new schema/index or production data mutation is required; bounded result sizes alone are not a fleet-scale query-plan/performance claim.

Photos are explicit public display DTOs; failed signing retains caption/date with `url: null`, never an internal fallback. An independently shared photo linked to an unsent same-scope report remains visible. The direct ChildMedia child owns its display name; any linked report must independently satisfy the same family/school predicates. Upload validation prevents a different-child link; historical same-family sibling contradictions are not expanded into another family's data. Signed URLs are not persisted in evidence.

The mobile date control has fixed small gutters, full readable dates and 44px targets at 200% text. Key report notes, supplies, mood/counts and attendance stay visible; complete care details use a keyboard-operable native disclosure. No care array is truncated. Photo rows have tighter spacing. Pagination preserves the other stream, checks complete receipts and older-than-anchor ordering, serializes duplicate attempts, rejects stale family/day/snapshot responses and retains content/cursors on failure. Focus moves to the first new item only if the initiating button still owns it; a valid empty continuation falls back to the loaded anchor. Preview fetching remains disabled.

Independent actual-Next testing reproduced and fixed two browser defects: WebKit's focused date could sit behind short-phone navigation, and Chromium restored a previous submitted native date on Back while showing a different server day. The date uses the existing active-only, non-focus-stealing visibility helper after two frames, with short-screen document-flow header clearance. Date-only `autocomplete=off` prevents stale native form restoration; no pageshow/popstate draft-reset effect is installed. The teacher helper export remains compatible and shares the same tested geometry implementation.

## Verification so far

- Actual-handler tests prove 101 equal-time reports and photos in 50/50/1 pages, 26 reachable nonempty days, DST 23/25-hour windows, capture dates, current/removed/mixed scopes, withdrawn cursors, signing failures and explicit DTO fields. Focused tests also prove initial-family mismatch is suppressed before effects, route gating, Home uncertainty, private projections and safe date links.
- The dedicated real-component fixture passes 24 cases in Chromium and 24 in WebKit, each with 32 locally intercepted fake GETs and zero product writes, blocked requests or client errors. Coverage includes twelve malformed/rejected/network response classes with same-cursor retry; held duplicate/cross-stream requests; stale family/day/disabled responses; terminal empty-page focus; synthetic photo rendering, alt text and full-size opening; date clearing and preview no-request behavior. All deferred request waits are bounded. Evidence: `output/playwright/parent-updates-history-{chromium,webkit}/results.json`.
- Broad existing recovery fixtures pass in both engines, preserving timezone/day content assertions and 30 intercepted synthetic attempts per engine. Existing daily-report tests now explicitly require notes/supplies/attendance outside the care disclosure and full care arrays inside; privacy and invoice-timezone assertions follow the stronger shared reader rather than obsolete source strings.
- Original-viewport actual-Next visual runs pass 24 cases at 320/390 and 100/200% with complete date/name text, 44px controls and shell clearance. Strict server-backed history Back/Forward/reload validation is separately running against a development-only, fixed fake three-day preview guarded from production and API use. Failed intermediate evidence is retained.
- `mobile:store:check` and TypeScript pass. Final full production gate and protected release remain pending. The first full gate exposed three obsolete source assertions; each was updated to the intended new behavior with stronger scope/content checks, not removed. The new guarded production mode adds only four exact-path/query, one-shot fake-authenticated GET probes plus read-only SSR date/navigation checks; all previous teacher checks remain, and 37 offline safety tests pass.

Native signatures, device behavior, archive, TestFlight, Apple submission and broader management-role authentication are not implied by these browser checks. Unrelated primary-checkout work, other PRs, stashes and source exports remain preserved.

### Final actual-Next navigation evidence

Both browser engines pass all eight original-viewport sequences, with 168 verified navigation states (84 per engine), on the same stable parent source `db45299a` and preview source `615e6fef`. Chromium finished at 21:07:07.122 UTC and WebKit at 21:08:15.966 UTC. Exact selected day, report contents and date control agree through Back, Forward, reload, an explicit empty date, Earlier/Later/Latest, invalid-date recovery and leaving/returning through Messages. Home's September 11 report shortcut opens that report despite a September 12 photo-only latest day. Keyboard disclosures, 44px controls, focused date clearance and zero overflow pass at 320/390px and 100/200% text. Zero API, non-GET or off-origin requests and zero exceptions. Evidence: `output/playwright/parent-updates-history-next-final-{chromium,webkit}/results.json` and 64 fake-data screenshots. The initial Chromium mismatch and minimal autocomplete experiment are retained separately, not reported as passing runs.

### Production-gate candidate

The exact final candidate `2770298d77ba5b86b8b75bf35b6a1bee55fc84d3` passed `npm run vercel-build`: Prisma generation, lint (zero errors, one existing unrelated unused-variable warning), TypeScript, all 2,179 tests and the optimized Next.js 16.3.4 production build. Focused release suite: 54 passed. Both 24-case fixtures passed again for the final singular/plural copy. Exact-source actual-Next reruns passed eight sequences/168 states, all 144 summary-wording checks, and zero unsafe requests or browser exceptions. Both engines confirmed stable parent source `3a0426752b095f8ff28c1fbb93d405293948993641fc934b95b598136165d3e7`; final Chromium ended 21:12:31 UTC and WebKit 21:13:44 UTC. Fresh results and 64 fake screenshots: `output/playwright/parent-updates-history-release-candidate-{chromium,webkit}`. No tests, type/lint rules or security controls were disabled.

Protected PR #369 merged at 21:17:47 UTC as main `8ad3169c92a00f978d6b5171af57f1246414f946`, after successful CI run `34719256401`, CodeQL, exact-candidate Ready preview and zero unresolved review threads. Independent final review found no blocker. Production `dpl_5SgPcJxjEgnX6FWnoqTb7TPF8BDc` is Ready on all five canonical aliases at 21:20:43 UTC. Health/database connected at 21:21:44 UTC; build error filtering showed only the build-completion line. Guarded authenticated live verification is running and remains a separate required release gate.
